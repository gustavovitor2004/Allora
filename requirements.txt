# yt-dlp e uma ferramenta EXTERNA, como ffmpeg e poppler - ela NAO vai
# empacotada no .exe (ver --exclude-module em build_exe.ps1). Instalar
# via pip aqui e conveniencia para rodar do fonte: o pip coloca o
# yt-dlp.exe no PATH, que e onde utils.find_yt_dlp() procura. O build
# distribuido usa tools/yt-dlp/yt-dlp.exe, atualizavel com `yt-dlp.exe -U`.
yt-dlp>=2024.8.6
PySide6>=6.6.0
requests>=2.31.0

# Aba "Documentos" - Digitalizar (scanner: correcao de perspectiva + realce via OpenCV)
opencv-python-headless>=4.8.0
numpy>=1.24.0

# Aba "Documentos" - Converter Formato (conversao de arquivos)
Pillow>=10.0.0
pdf2image>=1.16.3
pdfplumber>=0.10.0
pdf2docx>=0.5.6
python-docx>=1.1.0
reportlab>=4.0.0
docx2pdf>=0.1.8
pypdf>=4.0.0
