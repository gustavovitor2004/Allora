"""
version.py

Single source of truth for the app's own version number, shown in the
About dialog. Bump APP_VERSION by hand whenever a new release is
published - it isn't derived automatically from anything (there's no
build step that stamps it in).
"""

APP_VERSION = "1.2.0"
